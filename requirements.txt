# Third-party packages actually imported by src/ and models/ (stdlib excluded),
# pinned to the versions verified in the development venv (.venv, Python 3.12).
joblib==1.6.0
matplotlib==3.11.1
numpy==2.5.2
pandas==3.0.5
PyYAML==6.0.3
scikit-learn==1.9.0
